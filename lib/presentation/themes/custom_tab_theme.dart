import 'package:flutter/material.dart';

class CustomTabBarTheme extends TabBarTheme {
  const CustomTabBarTheme({required ColorScheme colorScheme})
      : super(
          indicatorSize: TabBarIndicatorSize.tab,
        );
}
